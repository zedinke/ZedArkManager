<Window x:Class="ZedASAManager.Views.RenameClusterWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        x:Name="RenameClusterWindowControl"
        Height="200" Width="400"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" 
                   x:Name="TitleTextBlock"
                   FontSize="18"
                   FontWeight="Bold"
                   Margin="0,0,0,16"/>

        <TextBlock Grid.Row="1" 
                   x:Name="ClusterNameLabel"
                   Margin="0,0,0,8"/>

        <TextBox Grid.Row="2" 
                 x:Name="ClusterNameTextBox"
                 Margin="0,0,0,16"
                 Padding="8"
                 KeyDown="ClusterNameTextBox_KeyDown"
                 GotFocus="ClusterNameTextBox_GotFocus"/>

        <StackPanel Grid.Row="4" 
                    Orientation="Horizontal" 
                    HorizontalAlignment="Right">
            <Button x:Name="RenameButton"
                    Content="Átnevezés"
                    Padding="12,6"
                    Margin="0,0,8,0"
                    Click="Rename_Click"
                    IsDefault="True"/>
            <Button x:Name="CancelButton"
                    Content="Mégse"
                    Padding="12,6"
                    Click="Cancel_Click"
                    IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>
